/* tslint:disable */
/* eslint-disable */
/**
 * Paraph API
 * Document API for developers. Upload PDF templates, fill them via API, and send for signing when you need to. Both workflows use the same endpoint.  ## Quick Start  Upload a PDF template, then create requests against it.  All requests require `Authorization: Bearer <your-api-key>`.  ### Fill a PDF  1. [Upload a template](#tag/templates/POST/templates) with a PDF that has named form fields. The response includes the template `id` and the detected fields. 2. [Create a request](#tag/requests/POST/requests) with the `template_id` and a `fields` map. Status is `success` immediately. 3. [Download the filled PDF](#tag/requests/GET/requests/{id}/download).  ---  ### Fill + send for signing  Same as above, but add signers. You\'ll need to set up signature placements on the template first (one-time).  1. [Upload a template](#tag/templates/POST/templates). 2. [Update the template](#tag/templates/PUT/templates/{id}) with `signature_placements` (page, coordinates, and which signer label goes where). 3. [Create a request](#tag/requests/POST/requests) with a `fields` map and a `signers` map keyed by label. Each signer gets an email with a signing link. Status is `pending` until everyone signs. 4. [Download the signed PDF](#tag/requests/GET/requests/{id}/download) once all signers have completed.  Use [webhooks](#tag/webhooks) to get notified instead of polling.  ---  ### Pre-signed documents  If you already have a signer\'s signature image (captured in your own app, for example), pass `override_signature_url` in the `SignerInput` when [creating a request](#tag/requests/POST/requests). That signer is marked as signed immediately and no email is sent. You can mix this with regular email-based signing on the same request.  ---  ### Webhooks  1. [Create a webhook](#tag/webhooks/POST/webhooks) with a URL and the events you care about. 2. Each delivery is a POST with `Content-Type: application/json`. See the `WebhookDelivery` schema for the payload format. 3. Verify deliveries using the `secret` returned at creation time. Each delivery includes an `X-Signature-256` header with an HMAC-SHA256 signature.  ---  ### Metadata  Pass a `metadata` object (up to 10 key-value pairs) when [creating a request](#tag/requests/POST/requests) to tag it with your own identifiers. Metadata is returned on all [request endpoints](#tag/requests) and in webhook deliveries.  ---  ### Salesforce integration  Paraph offers a managed package on the Salesforce AppExchange. The SF package talks to this same API — no special endpoints, no separate auth.  **Setup (one-time per SF org):**  1. In Paraph: create an API key (Admin → API Keys). Name it something    like \"Salesforce production\". 2. In Salesforce: open the paraph managed package\'s setup wizard and    paste the API key. The package stores it in a Named Credential. 3. Done. Every Apex callout auto-injects `Authorization: Bearer <key>`    via the Named Credential\'s custom header.  **What Apex code looks like:**  ```apex HttpRequest req = new HttpRequest(); req.setEndpoint(\'callout:Paraph_Named_Credential/api/v1/requests\'); req.setMethod(\'POST\'); req.setHeader(\'Content-Type\', \'application/json\'); req.setBody(payload); HttpResponse res = new Http().send(req); ```  **Rotation:** create a second API key, paste the new one into SF, delete the old one from Paraph. Both are valid simultaneously so there is no downtime window.  **Revocation:** delete the key in Paraph. The next SF callout fails with `401`; the admin pastes a fresh key.  ---  ### Common headers  All API responses include: - `X-Request-Id` — unique identifier for the request, useful for debugging and support. - `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` — rate limit status. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@paraph.dev
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateRequestRequest,
  RequestListResponse,
  RequestResponse,
  RequestStatus,
} from '../models/index';
import {
    CreateRequestRequestFromJSON,
    CreateRequestRequestToJSON,
    RequestListResponseFromJSON,
    RequestListResponseToJSON,
    RequestResponseFromJSON,
    RequestResponseToJSON,
    RequestStatusFromJSON,
    RequestStatusToJSON,
} from '../models/index';

export interface CancelRequestRequest {
    id: string;
}

export interface CreateRequestOperationRequest {
    createRequestRequest: CreateRequestRequest;
    idempotencyKey?: string;
}

export interface DownloadRequestRequest {
    id: string;
}

export interface GetRequestRequest {
    id: string;
}

export interface ListRequestsRequest {
    page?: number;
    pageSize?: number;
    status?: RequestStatus;
    from?: Date;
    to?: Date;
    metadataKey?: string;
    metadataValue?: string;
}

/**
 * 
 */
export class RequestsApi extends runtime.BaseAPI {

    /**
     * Creates request options for cancelRequest without sending the request
     */
    async cancelRequestRequestOpts(requestParameters: CancelRequestRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling cancelRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/requests/{id}/cancel`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Cancels all pending signers on the request and sets the request status to `cancelled`. Signers who have already signed are not affected. Only requests with status `pending` can be cancelled. 
     * Cancel request
     */
    async cancelRequestRaw(requestParameters: CancelRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RequestResponse>> {
        const requestOptions = await this.cancelRequestRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RequestResponseFromJSON(jsonValue));
    }

    /**
     * Cancels all pending signers on the request and sets the request status to `cancelled`. Signers who have already signed are not affected. Only requests with status `pending` can be cancelled. 
     * Cancel request
     */
    async cancelRequest(requestParameters: CancelRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RequestResponse> {
        const response = await this.cancelRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createRequest without sending the request
     */
    async createRequestRequestOpts(requestParameters: CreateRequestOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createRequestRequest'] == null) {
            throw new runtime.RequiredError(
                'createRequestRequest',
                'Required parameter "createRequestRequest" was null or undefined when calling createRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/requests`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateRequestRequestToJSON(requestParameters['createRequestRequest']),
        };
    }

    /**
     * Fills the template\'s form fields and creates a request.  If `signers` is provided and the template has signature placements configured, each signer receives a signing link via email. To provide a signer\'s signature directly (skipping the email), use `override_signature_url` in the signer object.  Omitting `signers` produces a fill-only PDF with no signing flow.  Use `GET /requests/{id}/download` to retrieve the filled or signed PDF. 
     * Create request
     */
    async createRequestRaw(requestParameters: CreateRequestOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RequestResponse>> {
        const requestOptions = await this.createRequestRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RequestResponseFromJSON(jsonValue));
    }

    /**
     * Fills the template\'s form fields and creates a request.  If `signers` is provided and the template has signature placements configured, each signer receives a signing link via email. To provide a signer\'s signature directly (skipping the email), use `override_signature_url` in the signer object.  Omitting `signers` produces a fill-only PDF with no signing flow.  Use `GET /requests/{id}/download` to retrieve the filled or signed PDF. 
     * Create request
     */
    async createRequest(requestParameters: CreateRequestOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RequestResponse> {
        const response = await this.createRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for downloadRequest without sending the request
     */
    async downloadRequestRequestOpts(requestParameters: DownloadRequestRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling downloadRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/requests/{id}/download`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the filled PDF. If all signers have completed, signatures are applied to the PDF. If signing is still pending, you get the filled PDF without signatures. 
     * Download request PDF
     */
    async downloadRequestRaw(requestParameters: DownloadRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.downloadRequestRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Returns the filled PDF. If all signers have completed, signatures are applied to the PDF. If signing is still pending, you get the filled PDF without signatures. 
     * Download request PDF
     */
    async downloadRequest(requestParameters: DownloadRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.downloadRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getRequest without sending the request
     */
    async getRequestRequestOpts(requestParameters: GetRequestRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/requests/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the request with its field inputs and signer statuses.
     * Get request detail
     */
    async getRequestRaw(requestParameters: GetRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RequestResponse>> {
        const requestOptions = await this.getRequestRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RequestResponseFromJSON(jsonValue));
    }

    /**
     * Returns the request with its field inputs and signer statuses.
     * Get request detail
     */
    async getRequest(requestParameters: GetRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RequestResponse> {
        const response = await this.getRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listRequests without sending the request
     */
    async listRequestsRequestOpts(requestParameters: ListRequestsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['page_size'] = requestParameters['pageSize'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = (requestParameters['from'] as any).toISOString();
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = (requestParameters['to'] as any).toISOString();
        }

        if (requestParameters['metadataKey'] != null) {
            queryParameters['metadata_key'] = requestParameters['metadataKey'];
        }

        if (requestParameters['metadataValue'] != null) {
            queryParameters['metadata_value'] = requestParameters['metadataValue'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/requests`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns all requests in your account, newest first.
     * List requests
     */
    async listRequestsRaw(requestParameters: ListRequestsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RequestListResponse>> {
        const requestOptions = await this.listRequestsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RequestListResponseFromJSON(jsonValue));
    }

    /**
     * Returns all requests in your account, newest first.
     * List requests
     */
    async listRequests(requestParameters: ListRequestsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RequestListResponse> {
        const response = await this.listRequestsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
